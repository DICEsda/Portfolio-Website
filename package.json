{
  "private": true,
  "name": "portfolio-root-wrapper",
  "scripts": {
    "build": "cd portfolio && npm install && npm run build",
    "dev": "cd portfolio && npm run dev",
    "lint": "cd portfolio && npm run lint"
  },
  "devDependencies": {
    "@remix-run/dev": "^1.0.0",
    "gh-pages": "^6.3.0"
  }
}
